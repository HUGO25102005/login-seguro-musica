# Supabase — valores públicos (seguros para el cliente)
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxx

# URL pública del sitio (para OAuth redirects en producción)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Solo para scripts/ingest.ts — NUNCA exponer al cliente, NUNCA commitear con valor real
SUPABASE_SERVICE_ROLE_KEY=
